# Template archive

Design backups and unused Stitch variants. **Not served by Flask routes.**

Prefer live files under `templates/` (and `templates/components/`). Do not wire routes to this folder without an intentional product decision.
